version: '3'
services:
  web:
    build: .
    ports:
      - "8083:8080"
    links:
      - mongo
    environment:
      - DB_URL=mongodb://mongo/blog
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - /var/lib/mongodb:/data/db
